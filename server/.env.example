#
# Example environment variables for the backend service.
# Copy to `.env.local` for local development.
#

# Port the Node/Express server listens on
PORT=4000

# GraphQL HTTP and WebSocket endpoint path
# Example values: /graphql, /api/graphql
GRAPHQL_PATH=/graphql

